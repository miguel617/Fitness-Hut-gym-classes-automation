name: Scheduled Docker Job

on:
  # Allow the workflow to be manually triggered
  workflow_dispatch:

  # Schedule runs at specific times
  schedule:
    - cron: "0 19 * * 0"    # Runs 48 hours less (UTC time) to schedule Tuesday : 19:00 - V-Power
    - cron: "0 18 * * 1"    # Runs 48 hours less (UTC time) to schedule Wednesday : 18:00 - V-Power
    - cron: "45 18 * * 1"    # Runs 48 hours less (UTC time) to schedule Wednesday : 18:45 - V-Burn
    - cron: "30 17 * * 2"    # Runs 48 hours less (UTC time) to schedule Thursday : 17:30 - V-Power
    - cron: "30 19 * * 3"    # Runs 48 hours less (UTC time) to schedule Friday : 19:30 - V-Power

jobs:
  run-docker:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker image
        run: |
          docker build -t fh-automation-image .

      - name: Run Docker container
        run: |
          docker run \
            -e SMTP_USER=${{ secrets.SMTP_USER }} \
            -e SMTP_PASSWORD=${{ secrets.SMTP_PASSWORD }} \
            -e FH_USERNAME=${{ secrets.FH_USERNAME }} \
            -e FH_PWD=${{ secrets.FH_PWD }} \
            fh-automation-image